apiVersion: apps/v1
kind: Deployment
metadata:
  name: masterlion
spec:
  template:
    spec:
      containers:
        - name: masterlion
          envFrom:
            - secretRef:
                name: masterlion-secret
            - configMapRef:
                name: masterlion-test-config
